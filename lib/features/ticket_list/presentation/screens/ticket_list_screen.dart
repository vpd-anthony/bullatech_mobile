import 'package:flutter/material.dart';

class TicketListScreen extends StatelessWidget {
  const TicketListScreen({
    super.key,
  });

  @override
  Widget build(final BuildContext context) {
    return const Center(
      child: Text('Employee Widgets'),
    );
  }
}
